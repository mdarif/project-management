# name: Deploy to GitHub Pages

# # When a pull request is merged to the 'master' branch or someone pushes to the 'master' branch directly, it will be built and deployed to the 'gh-pages' branch. After that, the new build output will be served on the GitHub Pages site. This job will be called deploy.

# on:
#   push:
#     branches:
#       - master
#       - gh-pages
#     # Review gh actions docs if you want to further define triggers, paths, etc
#     # https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#on

# jobs:
#   deploy:
#     name: Deploy to GitHub Pages
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v2
#       - uses: actions/setup-node@v3
#         with:
#           node-version: 18
#           cache: npm

#       - name: Install dependencies
#         working-directory: ./website
#         run: npm install --frozen-lockfile
        
#       - name: Build website
#         working-directory: ./website
#         run: npm run build

#       # Popular action to deploy to GitHub Pages:
#       # Docs: https://github.com/peaceiris/actions-gh-pages#%EF%B8%8F-docusaurus
#       - name: Deploy to GitHub Pages
#         uses: peaceiris/actions-gh-pages@v3
#         with:
#           github_token: ${{ secrets.GITHUB_TOKEN }}
#           # Build output to publish to the `gh-pages` branch:
#           publish_dir: ./website/build
#           # destination_dir: subdir # A destination subdirectory on a publishing branch. The default is empty.
#           # The following lines assign commit authorship to the official
#           # GH-Actions bot for deploys to `gh-pages` branch:
#           # https://github.com/actions/checkout/issues/13#issuecomment-724415212
#           # The GH actions bot is used by default if you didn't specify the two fields.
#           # You can swap them out with your own user credentials.
#           user_name: mdarif
#           user_email: arif_iq@yahoo.co.in


# Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to GH Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches:
      - master
      - gh-pages

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  # Single deploy job since we're just deploying
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup Pages
        uses: actions/configure-pages@v1
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v1
        with:
          # Upload entire repository
          path: './website/build'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@main